apiVersion: v1
kind: Service
metadata:
  name: {{ include "noc-ran.fullname" . }}
  labels:
    {{- include "noc-ran.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "noc-ran.selectorLabels" . | nindent 4 }}
